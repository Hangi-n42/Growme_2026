# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Growme_2026"

[setup]
script = '''
if (Test-Path -LiteralPath "package.json") {
  $corepackHome = Join-Path ([System.IO.Path]::GetTempPath()) "codex-corepack"
  New-Item -ItemType Directory -Force -Path $corepackHome | Out-Null
  $env:COREPACK_HOME = $corepackHome

  corepack pnpm@9.15.0 install --frozen-lockfile
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  corepack pnpm@9.15.0 exec playwright install --with-deps chromium
  if ($LASTEXITCODE -ne 0) {
    Write-Warning "Playwright browser/dependency installation failed; continuing."
    $global:LASTEXITCODE = 0
  }
}
'''
