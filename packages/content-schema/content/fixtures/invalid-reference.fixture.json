{
  "description": "Unresolved item references are rejected.",
  "patches": [
    { "op": "replace", "path": ["recipes", 0, "inputs", 0, "itemId"], "value": "missing_item" }
  ],
  "expectedErrors": ["recipe repair_plank_recipe.inputs[0].itemId references unknown id: missing_item"]
}
