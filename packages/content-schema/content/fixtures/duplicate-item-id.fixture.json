{
  "description": "Duplicate item IDs are rejected.",
  "patches": [
    { "op": "replace", "path": ["items", 1, "id"], "value": "turnip_seed" }
  ],
  "expectedErrors": ["Duplicate item id: turnip_seed"]
}
